<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>Zayrah | Add Product</title>
  <link rel="shortcut icon" type="image/x-icon" href="/favicon.ico?v=1" />
  <link rel="stylesheet" href="/css/admin/products/add.css" />
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.css" />
</head>
<body>
  <div class="dashboard">
    <!-- ── Sidebar ── -->
    <aside class="sidebar" id="sidebar">
      <div class="sidebar__brand">
        <div class="brand-mark">Z</div>
        <div>
          <h2><span class="brand-name">Zayrah</span> Dashboard</h2>
          <p>Admin panel</p>
        </div>
      </div>

      <nav class="sidebar__nav">
        <a href="/admin/dashboard" class="nav-link">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="7" height="7" rx="1"/><rect x="14" y="3" width="7" height="7" rx="1"/><rect x="3" y="14" width="7" height="7" rx="1"/><rect x="14" y="14" width="7" height="7" rx="1"/></svg>
          Overview
        </a>
        <a href="/admin/products" class="nav-link active">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 01-2.83 0L2 12V2h10l8.59 8.59a2 2 0 010 2.82z"/><line x1="7" y1="7" x2="7.01" y2="7"/></svg>
          Product Management
        </a>
        <a href="/admin/categories" class="nav-link">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 01-2 2H4a2 2 0 01-2-2V5a2 2 0 012-2h5l2 3h9a2 2 0 012 2z"/></svg>
          Category Management
        </a>
        <a href="/admin/settings" class="nav-link">
          <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="3"/><path d="M19.4 15a1.65 1.65 0 00.33 1.82l.06.06a2 2 0 010 2.83 2 2 0 01-2.83 0l-.06-.06a1.65 1.65 0 00-1.82-.33 1.65 1.65 0 00-1 1.51V21a2 2 0 01-4 0v-.09A1.65 1.65 0 009 19.4a1.65 1.65 0 00-1.82.33l-.06.06a2 2 0 01-2.83-2.83l.06-.06A1.65 1.65 0 004.68 15a1.65 1.65 0 00-1.51-1H3a2 2 0 010-4h.09A1.65 1.65 0 004.6 9a1.65 1.65 0 00-.33-1.82l-.06-.06a2 2 0 012.83-2.83l.06.06A1.65 1.65 0 009 4.68a1.65 1.65 0 001-1.51V3a2 2 0 014 0v.09a1.65 1.65 0 001 1.51 1.65 1.65 0 001.82-.33l.06-.06a2 2 0 012.83 2.83l-.06.06A1.65 1.65 0 0019.4 9a1.65 1.65 0 001.51 1H21a2 2 0 010 4h-.09a1.65 1.65 0 00-1.51 1z"/></svg>
          Settings
        </a>
        <a href="/admin/logout" class="nav-link" style="color: var(--red); border: 1px solid rgba(138, 47, 47, 0.2); background: rgba(138, 47, 47, 0.04); margin-top: 20px; font-weight: 600;">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" width="16" height="16" style="margin-right: 8px; vertical-align: middle;"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"/><polyline points="16 17 21 12 16 7"/><line x1="21" y1="12" x2="9" y2="12"/></svg>
          Logout
        </a>
      </nav>
    </aside>

    <div class="overlay" id="overlay"></div>

    <!-- ── Main Content ── -->
    <main class="main">
      <header class="topbar">
        <button class="menu-btn" id="menuBtn" aria-label="Open menu">
          <span></span>
          <span></span>
          <span></span>
        </button>

        <div class="topbar__title">
          <h1>Add Product</h1>
          <p>Add a new piece to your collection</p>
        </div>


      </header>

      <!-- Breadcrumb -->
      <nav class="breadcrumb" aria-label="Breadcrumb">
        <a href="/admin/products" class="breadcrumb__link">Product Management</a>
        <span class="breadcrumb__sep">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><polyline points="9 18 15 12 9 6"/></svg>
        </span>
        <span class="breadcrumb__current">Add New</span>
      </nav>

      <!-- Form Card -->
      <section class="form-card">
        <div class="form-card__header">
          <div class="form-card__icon">
            <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 01-2.83 0L2 12V2h10l8.59 8.59a2 2 0 010 2.82z"/><line x1="7" y1="7" x2="7.01" y2="7"/></svg>
          </div>
          <div>
            <h2 class="form-card__title">Product Details</h2>
            <p class="form-card__subtitle">Provide details about the item</p>
          </div>
        </div>

        <% if (typeof error !== 'undefined' && error) { %>
          <div class="alert alert--error">
            <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><circle cx="12" cy="12" r="10"/><line x1="12" y1="8" x2="12" y2="12"/><line x1="12" y1="16" x2="12.01" y2="16"/></svg>
            <span><%= error %></span>
          </div>
        <% } %>

        <form action="/admin/products/add" method="POST" enctype="multipart/form-data" id="productForm" novalidate>

          <div class="form-grid">
            <!-- Left Column: Primary Details -->
            <div class="form-column">
              <!-- Product Name -->
              <div class="field" id="nameField">
                <label class="field__label" for="productName">Product Name <span class="required">*</span></label>
                <div class="field__wrap">
                  <span class="field__icon">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M20.59 13.41l-7.17 7.17a2 2 0 01-2.83 0L2 12V2h10l8.59 8.59a2 2 0 010 2.82z"/><line x1="7" y1="7" x2="7.01" y2="7"/></svg>
                  </span>
                  <input type="text" class="field__input" id="productName" name="name" placeholder="e.g. Silk Organza Anarkali" required autocomplete="off" />
                </div>
                <span class="field__error-msg" id="nameError">Product name is required</span>
              </div>

              <!-- Category Dropdown -->
              <div class="field" id="categoryField">
                <label class="field__label" for="productCategory">Category <span class="required">*</span></label>
                <div class="field__wrap">
                  <span class="field__icon">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M22 19a2 2 0 01-2 2H4a2 2 0 01-2-2V5a2 2 0 012-2h5l2 3h9a2 2 0 012 2z"/></svg>
                  </span>
                  <select class="field__input" id="productCategory" name="category" required>
                    <option value="" disabled selected>Select category…</option>
                    <% if (categories && categories.length > 0) { %>
                      <% categories.forEach(function(cat) { %>
                        <option value="<%= cat._id %>"><%= cat.name %></option>
                      <% }); %>
                    <% } %>
                  </select>
                </div>
                <span class="field__error-msg" id="categoryError">Please select a category</span>
                <% if (!categories || categories.length === 0) { %>
                  <p class="category-warning">⚠️ No listed categories found. Please <a href="/admin/categories/add">create a category</a> first.</p>
                <% } %>
              </div>

              <!-- Description -->
              <div class="field">
                <label class="field__label" for="productDesc">Description</label>
                <div class="field__wrap field__wrap--textarea">
                  <span class="field__icon field__icon--textarea">
                    <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="17" y1="10" x2="3" y2="10"/><line x1="21" y1="6" x2="3" y2="6"/><line x1="21" y1="14" x2="3" y2="14"/><line x1="17" y1="18" x2="3" y2="18"/></svg>
                  </span>
                  <textarea class="field__textarea" id="productDesc" name="description" placeholder="Enter product descriptions, materials used, size options…" rows="5" maxlength="1000"></textarea>
                </div>
                <div class="char-count"><span id="charCurrent">0</span> / 1000 characters</div>
              </div>

              <!-- Dynamic Category Variants -->
              <div id="variantsContainer"></div>
              <div id="combinationsTableContainer" style="margin-top: 24px;"></div>
            </div>

            <!-- Right Column: Numbers & Stock -->
            <div class="form-column">
              <!-- Pricing Row -->
              <div class="fields-row">
                <!-- Original Price -->
                <div class="field" id="priceField">
                  <label class="field__label" for="productPrice">Price (₹) <span class="required">*</span></label>
                  <div class="field__wrap">
                    <span class="field__icon">
                      <strong>₹</strong>
                    </span>
                    <input type="number" class="field__input" id="productPrice" name="price" placeholder="9999" min="0" required autocomplete="off" />
                  </div>
                  <span class="field__error-msg" id="priceError">Valid price is required</span>
                </div>

                <!-- Discount Price -->
                <div class="field" id="discountField">
                  <label class="field__label" for="productDiscount">Discount Price (₹)</label>
                  <div class="field__wrap">
                    <span class="field__icon">
                      <strong>₹</strong>
                    </span>
                    <input type="number" class="field__input" id="productDiscount" name="discountPrice" placeholder="e.g. 7999" min="0" autocomplete="off" />
                  </div>
                  <span class="field__error-msg" id="discountError">Discount price must be less than original price</span>
                </div>
              </div>



              <!-- Multiple Images Upload -->
              <div class="field">
                <label class="field__label">Product Images <span class="muted">(Min 3, Max 5)</span></label>
                <div class="upload-zone" id="uploadZone">
                  <input type="file" name="images" id="imageInput" accept="image/*" multiple hidden />
                  <div class="upload-zone__content" id="uploadContent">
                    <div class="upload-zone__icon">
                      <svg width="40" height="40" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"><rect x="3" y="3" width="18" height="18" rx="2" ry="2"/><circle cx="8.5" cy="8.5" r="1.5"/><polyline points="21 15 16 10 5 21"/></svg>
                    </div>
                    <p class="upload-zone__text">Drag &amp; drop images, or <span class="upload-zone__link">browse</span></p>
                    <p class="upload-zone__hint">Supports JPG, PNG, WebP · Max 5MB per file</p>
                  </div>
                  
                  <!-- Preview Grid -->
                  <div class="upload-grid" id="uploadGrid" style="display: none;"></div>
                </div>
                <span class="field__error-msg" id="imagesError" style="display:none; margin-top: 8px;">At least one product image is required</span>
              </div>
            </div>
          </div>

          <!-- Toggle Switches -->
          <div class="toggles-row">
            <div class="toggle-group">
              <label class="toggle-group__label" for="isFeatured">
                <div>
                  <span class="toggle-group__title">Featured Product</span>
                  <span class="toggle-group__desc">Showcase this item on the main store page</span>
                </div>
              </label>
              <label class="toggle-switch">
                <input type="checkbox" name="isFeatured" id="isFeatured" />
                <span class="toggle-switch__slider"></span>
              </label>
            </div>

            <div class="toggle-group">
              <label class="toggle-group__label" for="isListed">
                <div>
                  <span class="toggle-group__title">Listed</span>
                  <span class="toggle-group__desc">Make this product visible to customers</span>
                </div>
              </label>
              <label class="toggle-switch">
                <input type="checkbox" name="isListed" id="isListed" checked />
                <span class="toggle-switch__slider"></span>
              </label>
            </div>
          </div>

          <!-- Form Actions -->
          <div class="form-actions">
            <a href="/admin/products" class="btn btn--secondary">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><line x1="19" y1="12" x2="5" y2="12"/><polyline points="12 19 5 12 12 5"/></svg>
              Cancel
            </a>
            <button type="submit" class="btn btn--primary" id="submitBtn">
              <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"><path d="M19 21H5a2 2 0 01-2-2V5a2 2 0 012-2h11l5 5v11a2 2 0 01-2 2z"/><polyline points="17 21 17 13 7 13 7 21"/><polyline points="7 3 7 8 15 8"/></svg>
              <span class="btn__text">Save Product</span>
            </button>
          </div>

        </form>
      </section>
    </main>
  </div>

  <!-- Image Crop Modal -->
  <div class="modal-overlay" id="cropModalOverlay">
    <div class="modal modal--crop">
      <h3>Crop Image</h3>
      <p style="margin-bottom: 16px; color: var(--muted); font-size: .92rem;">Adjust the selection to crop this product image (3:4 aspect ratio).</p>
      <div class="cropper-container">
        <img id="cropperImage" src="" alt="Source image to crop" />
      </div>
      <div class="modal__actions">
        <button type="button" class="btn btn--cancel" id="cancelCrop">Cancel</button>
        <button type="button" class="btn btn--confirm-crop" id="confirmCrop">
          <span class="btn__text">Crop &amp; Apply</span>
        </button>
      </div>
    </div>
  </div>

  <script src="https://cdnjs.cloudflare.com/ajax/libs/cropperjs/1.6.2/cropper.min.js"></script>
  <script>
    const categoriesData = <%- JSON.stringify(categories) %>;
    const productVariantsData = [];
    const productCombinationsData = [];
  </script>
  <script src="/js/admin/products/add.js"></script>
  <%- include('../partials/loader') %>
</body>
</html>
